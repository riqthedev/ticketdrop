# Vercel Environment Variables
# Copy these into Vercel Dashboard → Settings → Environment Variables
# 
# IMPORTANT: For DATABASE_URL, the password contains a $ character
# You may need to URL-encode it as %24 in Vercel, or use the connection pooler string from Supabase

# Database Connection (Supabase)
# Option 1: Direct connection (may need URL-encoding for $ character)
DATABASE_URL=postgresql://postgres:Nicetrybitch12$@db.bhkadniqimndnfvwaxxh.supabase.co:5432/postgres

# Option 2: If $ character causes issues, URL-encode it as %24:
# DATABASE_URL=postgresql://postgres:Nicetrybitch12%24@db.bhkadniqimndnfvwaxxh.supabase.co:5432/postgres

# Option 3: Use connection pooler (recommended - get from Supabase Dashboard → Settings → Database → Connection pooling)
# DATABASE_URL=postgresql://postgres.bhkadniqimndnfvwaxxh:YOUR_PASSWORD@aws-0-[region].pooler.supabase.com:6543/postgres

# Redis (if you have Upstash or other Redis service)
# REDIS_HOST=your-redis-host.upstash.io
# REDIS_PORT=6379
# Or use REDIS_URL if your provider supports it

# Application Secret
QR_SECRET=local-development-secret-key-change-in-production

# Optional: Frontend URL for CORS
# FRONTEND_URL=https://ticketdrop-phi.vercel.app

# Note: The following are for local development only, don't add to Vercel:
# PORT=4000
# NODE_ENV=development
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=ticketdrop
# DB_PASSWORD=ticketdrop
# DB_NAME=ticketdrop
# JWT_SECRET=replace_me_in_local_env
# QUEUE_SALT=replace_me_with_random_string


services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: ticketdrop
      POSTGRES_PASSWORD: ticketdrop
      POSTGRES_DB: ticketdrop
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"

volumes:
  db_data:

